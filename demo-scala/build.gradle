apply plugin: 'scala'

repositories {
    jcenter()
}

sourceSets {
    main {
        scala {
            srcDirs = ['src']
        }
    }
}

dependencies {
    compile rootProject,
            'org.scala-lang:scala-library:2.12.6'
}
