apply plugin: 'java'
apply plugin: 'findbugs'

sourceCompatibility = 1.8

repositories {
    jcenter()
}

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}

findbugs {
    effort = "max"
}

dependencies {
    compile rootProject
}
